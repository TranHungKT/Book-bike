import { StyleSheet } from 'react-native'

import { Normalize } from '@/Themes'

export default StyleSheet.create({
  buttonStyle: {
    width: '90%',
    position: 'absolute',
    bottom: Normalize(30)
  }
})
